<div class="container-fluid">
    <div class="jumbotron" style="width:95%;margin:auto;margin-top:2rem">

        <h1>Parent's Area</h1>
        <% if( currentUser) { %>
            <p>Name: <%= currentUser.fullName %></p>
            <p>Email: <%= currentUser.email %></p>
        <% } else { %>
            <p>No Current parent to show</p>
        <% } %>            
    </div>
</div>