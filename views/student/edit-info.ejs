<div>
    <% if(!currentUser) { %>
        <div class="jumbotron text-center" style="width:95%;margin:auto;margin-top:1rem">
            <h3 class="container" style="margin-top:2rem">Edit Account Info</h3>
            <p>No student is currently logged in.</p>
        </div>
    <% } else { %>
        <div  class="container text-center" style="margin-top:1rem">
            <h3>Edit Account Info</h3>
            <p>Current Values are displayed. Please enter new values.<br/>
            Email will be updated only if it is not in use by another user.<br/>
            Current Password is needed for any changes.
            </p>    
        </div>

        <div class="col" style="margin-top:1rem">
            <form name="form-sign-up" method="POST" action="/student/update-info">
                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputFirstName" style="width:11rem;text-align:right;">First Name:</label>
                    <input id="inputFirstName" type="text" name="first" style="width:55%;" value=<%= currentUser.name.first; %> >
                </div>
    
                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputLastName" style="width:11rem;text-align:right;">Last Name:</label>
                    <input id="inputLastName" type="text" name="last" style="width:55%;" value=<%= currentUser.name.last; %>>
                </div>
    
                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputEmail" style="width:11rem;text-align:right;">Email:</label>
                    <input id="inputEmail" type="email" name="email" style="width:55%;" value=<%= currentUser.email; %>>
                </div>

                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputPassword" style="width:11rem;text-align:right;">Current Password:</label>
                    <input id="inputPassword" type="password" name="password" placeholder="password..." style="width:55%;" required>
                </div>

                <div class="text-center" style="margin-top:1.2rem">
                    <input type="submit" value="Update" style="width:35%" class="btn btn-md btn-info">
                </div>
            </form>  
        </div>
    <% } %>
    </div>
</div>