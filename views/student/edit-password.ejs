<div>
    <% if(!loggedIn) { %>
        <div class="jumbotron text-center" style="width:95%;margin:auto;margin-top:1rem">
            <h3 class="container" style="margin-top:2rem">Edit Account Info</h3>
            <p>No student is currently logged in.</p>
        </div>
    <% } else { %>
        <div  class="container text-center" style="margin-top:1rem">
            <h3>Edit Password for <%= currentUser.fullName; %> </h3>
            <p>Current Password is needed to chnage it.<br/>
            To update password please enter it as New Password, <br/>
            and then enter it again to confirm it.
            </p>    
        </div>

        <div class="col" style="margin-top:1rem">
            <form name="form-sign-up" method="POST" action="/student/update-password">        
                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputPassword" style="width:11rem;text-align:right;">Current Password:</label>
                    <input id="inputPassword" type="password" name="password" placeholder="Password..." style="width:55%;" required>
                </div>
    
                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputNewPassword" style="width:11rem;text-align:right;">New Password:</label>
                    <input id="inputNewPassword" type="password" name="newPassword" placeholder="AlphaNumeric. 15 => Length => 8"  style="width:55%;" required>
                </div>

                <div class="text-center" style="margin-top:1.1rem">
                    <label for="inputConfirmNewPassword" style="width:11rem;text-align:right;">Confirm New Password:</label>
                    <input id="inputConfirmNewPassword" type="password" name="confirmNewPassword" placeholder="Confirm new password" style="width:55%;" required>
                </div>
    
                <div class="text-center" style="margin-top:1.2rem">
                    <input type="submit" value="Update" style="width:35%" class="btn btn-md btn-info">
                </div>
            </form>  
        </div>
    <% } %>
    </div>
</div>